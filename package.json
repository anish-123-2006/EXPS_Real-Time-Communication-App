{
  "name": "meshly-workspace",
  "private": true,
  "scripts": {
    "server:dev": "npm --prefix server run dev",
    "client:dev": "npm --prefix client run dev",
    "server:typecheck": "npm --prefix server run typecheck",
    "client:typecheck": "npm --prefix client run typecheck"
  }
}
